namespace _04_Modulo_Mas_sobre_TiposDatos.Codigo
{
    public class EArreglos
    {
        public void CodigoDelCurso()
        {
            char[] vocales = new char[] { 'a', 'e', 'i', 'o', 'u' };

            foreach(char vocal in vocales)
            {
                Console.WriteLine(vocal);
            }
        }
    }
}
